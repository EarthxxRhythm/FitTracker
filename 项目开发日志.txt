这是一个进度日志。请按时间倒序记录每个会话完成的工作。
[done] monetization hub + review history gating: turned monetization from shared-layer scaffolding into a real page flow by adding `MonetizationHubPage`, `MonetizationHubService`, review advanced-insights entry wiring, and Pro-gated full review history fallback logic in `ReviewInsightsService`. Membership focused smoke coverage was added in check-only mode for `review advanced insights entry -> membership hub` and local tier preview switching. Commits: `a620d98 feat: add monetization hub and gated review insights`, `482252d feat: gate full review history behind pro access`.
[done] monetization entitlement scaffolding: documented the first Free / Pro Lifetime / Plus Subscription packaging in `docs/monetization-spec-v1.md` and `docs/monetization-technical-design-v1.md`, added `MonetizationModels` plus catalog / entitlement / gate services, expanded `List.test.ets` with monetization coverage, and stabilized the preset run to `6 / 6 passed` on `127.0.0.1:5555` after fixing default user and invalid context failures. Commit: `7976290 feat: add monetization entitlement scaffolding`.
[done] local content sync package flow: moved Phase 4 content sync from design into implementation by adding bundled manifest/package generation, database import + fallback switching in `SyncService` / `ContentDatabaseService` / `ContentRepository`, new schema/import docs, and content-sync ohosTest coverage plus `tools/ohos-test.ps1` preset support. Commit: `f53d802 feat: implement local content sync package flow`.
[done] formal local backup closeout: promoted backup/restore from MVP utility to a formal local delivery package. Unified manual backup copy and restore semantics through `UserDataBackupService`, aligned `EntryBackupAbility.onProcess()` idle payload with the shared process JSON shape, added backup-specific Hypium coverage for invalid JSON, goal override, session dedupe merge, backup read failure, and idle/process payload formatting, then verified `node tools/check-gates.mjs`, `hvigorw assembleHap --mode module -p module=entry@default -p product=default --no-parallel`, `hvigorw assembleHap --mode module -p module=entry@ohosTest -p product=default --no-parallel`, and `tools/dev-smoke.ps1 -Target backup-card -DeviceId 127.0.0.1:5555`. Added delivery notes in `docs/backup-closeout.md`.
[done] MVP closeout package: finalized the internal acceptance package with unsigned HAP delivery, final acceptance records, rerun commands, and deferred-item boundaries in docs/mvp-closeout.md. Re-ran node tools/check-gates.mjs, hvigorw assembleHap for entry@default and entry@ohosTest, plus tools/auth-regression.ps1, tools/dev-smoke.ps1 -Target current-plan, and tools/dev-smoke.ps1 -Target both on 127.0.0.1:5555; all summaries passed and PackageHap -> spawn java ENOENT did not recur.
[done] current-plan workout flow closeout: fixed DevEco `PackageHap -> spawn java ENOENT` by hardening `tools/deveco-env.ps1` path normalization and adding repo-local Java shims (`tools/java.cmd`, `tools/node-java-shim.cjs`), then verified `hvigorw assembleHap --mode module -p module=entry@default -p product=default --no-parallel` and `module=entry@ohosTest` both pass. Added `current-plan` focused smoke coverage to `tools/dev-smoke.ps1` / `tools/midscene-entrypoints-smoke.ps1`, and confirmed `Home -> Preview -> Active` passes on `127.0.0.1:5555` with Midscene report output under `midscene_run/focused/current_plan-20260605-003730-p43936/`. Closed out current-plan page/service/ohosTest changes for Home, WorkoutPreview, and ActiveWorkout.
[done] workout-loop 14-20: added preset plan cards on Home, new TrainingPlanDetailPage route and activation flow, current-plan restore on reopen, and current-plan-driven today/rest view on Home. Verified with `node tools/check-gates.mjs`, `tools/task-relay.ps1` default and regression-file prompts, `hvigorw assembleHap entry@default`, and `hvigorw assembleHap entry@ohosTest`.
[done] DevEco env + focused smoke: added `tools/deveco-env.ps1`, switched `tools/task-relay.ps1` and `tools/check-gates.mjs` to reuse the same DevEco bootstrap, and rerouted `media-card` smoke through `Home -> Exercise Library -> Exercise Detail -> Media`. `node tools/check-gates.mjs` passed; `tools/dev-smoke.ps1 -Target media-card` and `-Target both` passed on `127.0.0.1:5555`, with summary and HTML report outputs saved under `midscene_run/focused/`.
[完成] backup bridge / media smoke 覆盖：提炼 `EntryBackupAbility` JSON 回调格式化 helper，新增 `SystemBackupBridgeService` 与 `ExerciseMediaResolverService` 的 ohosTest 用例，并补 `tools/dev-smoke.ps1` / `tools/dev-smoke.md` 作为 focused smoke 日常入口，提交 `1266059 test: cover backup bridge and media smoke entrypoints`。
[完成] focused smoke 路由稳定性：`tools/midscene-entrypoints-smoke.ps1` 新增前台拉回、英文 prompt、分步断言与 retry 路径，focused smoke 在模拟器入口漂移时更稳，提交 `f96a4ab fix: stabilize focused midscene smoke routes`。
[完成] 工具化：新增任务接力器 `tools/task-relay.ps1`，可自动选择下一项任务、采集 git 快照、运行固定前置检查，并生成可直接交给 Coding Agent 的下一步提示文件。
[完成] 下一阶段任务 #5：认证链路回归已跑通，补齐了 `tools/auth-regression.ps1`，并在 DevEco 模拟器上完成注册、关闭重开、会话恢复与首页/目标页分流验证。构建与 Midscene 真实回归通过。
[完成] 下一阶段任务 #4：模拟器视觉回归脚本已固化，补齐了 `tools/midscene-env.ps1`、回归说明和报告模板；训练执行首组录入、复盘、回顾和调整目标链路在 DevEco 模拟器上已跑通，当前可用模型为 `doubao-seed-2-0-lite-260215`，所有断言通过。
[修复] Midscene 回归脚本：补充本地 Markdown 摘要输出与失败排查说明，脚本 `CheckOnly` 已通过；真实视觉回归当前受 Midscene 接入点 404/429 限制阻塞。
[完成] 下一阶段任务 #3：PlanEngine 候选评分与训练预览解释性已收口，预览页现已展示每个动作的选中说明并保留兜底提示；构建与 ohosTest 编译通过。
[完成] 本地内容库启动接入：`StartupPage` 现在会先尝试加载本地数据库内容，失败时自动回退并重建 JSONL 种子库；`ContentRepository`、`ContentDatabaseService` 与导入说明已同步补齐。构建与 ohosTest 编译通过。
[完成] 阶段任务 #1：动作内容扩充到 50 条，并通过内容生成脚本完成覆盖校验；LocalExerciseContent.ets 已重新生成，覆盖报告显示目标、器械、肌群与难度均满足门槛。
[完成] 新阶段任务源已切换到 tasks.phase2.json：后续接力继续按单功能、单提交执行，任务说明和初始化脚本已同步指向新的阶段文件。
[完成] 页面治理 #6：收口 main_pages.json 的主注册链路，并同步更新根 AGENTS.md 与 pages/AGENTS.md 的页面地图，明确旧页面仅作历史素材保留。任务状态已更新为完成。
[完成] 这次模拟器视觉回归已闭环：在 DevEco 模拟器上跑通 启动 -> 目标设置 -> 首页 -> 训练预览 -> 训练执行 -> 复盘 -> 回顾 -> 调整目标 -> 目标设置 的完整链路；Midscene 在配额受限时改用 HDC/uitest 兜底，相关报告与截图保存在 midscene_run/。
[修复] Midscene 真实设备回归脚本：修复 PowerShell 参数展开与 device-id 传参问题；DevEco 模拟器 Pura 90 Pro Max 已通过 HDC 连接为 127.0.0.1:5555，HAP 安装、应用启动、截图和启动页视觉断言通过。完整训练链路因火山模型推理额度/安全体验模式 429 未继续执行。
[完成] 本地数据库读取方案：新增 ContentDatabaseService，支持将当前 JSONL 生成内容导入 RDB 并切换 ContentRepository 到 DatabaseContentDataSource；默认启动路径仍保留 JSONL 种子源。
[完成] 本地数据库导入脚本：新增 JSONL 到 SQL 种子导出脚本和 DatabaseContentDataSource，ContentRepository 支持切换内容源；导出脚本会先执行构建期内容校验。
[完成] 下一阶段任务 #4：固化 DevEco 模拟器/Midscene 视觉回归脚本与说明，覆盖启动、目标设置、首页、训练预览、训练执行、复盘、回顾和调整目标。构建与 ohosTest 编译通过；HDC 当前无可用设备，真实视觉链路未执行。
[完成] 下一阶段任务 #5：新增 ContentRepository 内容源适配接口，保留 JSONL 作为种子源，并补充本地数据库表结构设计。构建与 ohosTest 编译通过；未接入 MuscleWiki 官方 API。
[完成] 下一阶段任务 #3：PlanEngine 增加候选动作评分、难度/器械逐级兜底，并在训练预览页展示可解释兜底提示。构建与 ohosTest 编译通过；模拟器端到端测试未执行。
[完成] 下一阶段任务 #1/#2：动作内容扩充到 35 条，并为内容生成脚本增加覆盖检查和覆盖报告。构建与 ohosTest 编译通过；视频字段保持 local:// 本地占位，不接 MuscleWiki 官方 API。
[完成] 项目治理：统一任务源、归档旧任务并梳理旧页面分流。后续开发改读 tasks.next.json，旧 docs/tasks.json 已归档为 docs/tasks.legacy.json，新增旧页面治理清单。
[完成] 功能 #27：本地注册、登录与 token 会话恢复。构建与 ohosTest 编译通过；模拟器端到端测试未执行。
[完成] 功能 #26：本地偏好存储失败降级状态与默认数据。构建与 ohosTest 编译通过；模拟器端到端测试因无可控偏好存储失败注入未完整执行。
[完成] 功能 #25：动作历史个人纪录。构建与 ohosTest 编译通过；模拟器端到端测试因无可用历史记录未完整执行。
[完成] 功能 #24：本周训练统计和日历热力图。所有测试通过。

[完成] 功能 #21/#22：Hvigor 构建前自动校验动作 JSONL 并重新生成 ArkTS 内容模块，同时检查 main_pages 注册页文件和 @Entry 数量。构建与 ohosTest 编译通过；模拟器端到端测试因无可用设备未执行。

[完成] 功能 #20：PlanEngine 将每周训练天数限制在 0 到 7 天范围内。构建与 ohosTest 编译通过；模拟器端到端测试因无可用设备未执行。

[完成] 功能 #19：PlanEngine 按目标、器械、限制肌群和难度生成可训练计划。构建与 ohosTest 编译通过；模拟器端到端测试因无可用设备未执行。

[完成] 功能 #13/#15/#23：训练复盘动作最佳记录、回顾目标调整与计划重生成、个人资料偏好保存读取。构建与 ohosTest 编译通过；模拟器端到端测试因无可用设备未执行。

[完成] 功能 #11/#14/#18：训练会话本地保存、回顾最近记录与趋势、动作库多条件筛选。构建与 ohosTest 编译通过；模拟器端到端测试因无可用设备未执行。

[完成] 功能 #6/#10/#17：首页今日训练入口、训练执行 1RM 录入、动作详情本地内容展示。构建与 ohosTest 编译通过；模拟器端到端测试因无可用设备未执行。

[完成] 功能 #5/#9/#16：本周计划、训练执行状态、动作库本地展示。构建与 ohosTest 编译通过；模拟器端到端测试因无可用设备未执行。

[完成] 功能 #3/#4/#8：目标回填、首页目标摘要、训练预览开始训练路由。构建与 ohosTest 编译通过；模拟器端到端测试因无可用设备未执行。

[完成] 功能 #2/#7/#12：目标设置、训练预览、训练复盘并行收口。构建与 ohosTest 编译通过；模拟器端到端测试因无可用设备未执行。

[完成] 功能 #1：启动与路由。构建与 ohosTest 编译通过；模拟器端到端测试因无可用设备未执行。

[初始化] 项目环境已搭建。生成 tasks.json 文件，共包含 27 个功能点。
[done] content sync startup fallback hardening: routed `StartupPage` through `StartupContentSyncService`, added startup-level fallback orchestration coverage for empty database, failed bundled import, and pre-sync exception downgrade, extended `SyncService` manifest failure tests for `unsupported_kind` and `invalid_manifest`, and fixed `tools/ohos-test.ps1 -CheckOnly` so content-sync preset can validate without requiring HDC targets. Verified with `node tools/check-gates.mjs`, `hvigorw assembleHap --mode module -p module=entry@ohosTest -p product=default --no-parallel`, and `tools/ohos-test.ps1 -Preset content-sync -SkipBuild -SkipInstall -CheckOnly`.
[done] final validation hardening under HDC blocker: added `tools/hdc-targets.ps1`, taught `tools/ohos-test.ps1` / `tools/dev-smoke.ps1` / `tools/auth-regression.ps1` / `tools/midscene-entrypoints-smoke.ps1` / `tools/midscene-regression.ps1` to fail fast on unusable HDC targets, aligned `docs/blockers.md`, `docs/continuous-dev-loop.md`, and `tasks/project-queue.json` with the active `device.hdc_unavailable` blocker, then closed final repo-only validation gaps in `ContentDatabaseService`, `SessionManager`, and multiple ohosTest suites while hardening Midscene retry flow. Commits: `e97256d fix: harden hdc target checks for validation scripts`, `a10bbe8 chore: align closeout queue with HDC blocker state`, `5a182f9 fix: close final validation gaps and harden Midscene retries`.
[done] review monetization upgrade entry: added `ReviewUpgradeEntryService`, wired `ReviewHomePage` to expose the first upgrade guidance entry for monetization, covered it with `ReviewUpgradeEntryService.test.ets`, and advanced `tasks/project-queue.json` monetization lane planning. Commit: `8b0e89e feat: add review monetization upgrade entry`.
