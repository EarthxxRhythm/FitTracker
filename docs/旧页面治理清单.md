# 旧页面治理记录

## 当前结论

当前主线以 `main_pages.json` 和 `app/AppRoutes.ets` 为准，页面已迁移到 `features/pencil/`、`features/welcome/`、`features/onboarding/`、`features/exercise/`、`features/monetization/` 和 `features/workout/`。

以下旧代码已确认无路由、无 ArkTS import、无测试引用，并已从源码中移除：

- `pages/Index.ets`
- `pages/ProfilePage.ets`
- `pages/LoginPage.ets`
- `pages/RegisterPage.ets`
- `common/Constants.ets`
- `components/MainTabBar.ets`
- `components/Chip.ets`
- `components/EmptyState.ets`
- `components/SegmentedControl.ets`
- `components/StatBadge.ets`
- `features/review/pages/ReviewHomePage.ets`
- `features/workout/pages/TrainingPlanDetailPage.ets`

## 暂时保留

- `pages/HomePage.ets`：当前工作区正在修改的兼容包装，暂不与用户改动重叠。
- `features/workout/pages/WorkoutPreviewPage.ets`
- `features/workout/pages/ActiveWorkoutPage.ets`
- `features/workout/pages/WorkoutSummaryPage.ets`
- `features/workout/services/WorkoutSessionPersistenceBridgeService.ets`

其中训练页面和持久化桥仍可能被当前迁移工作或历史数据兼容逻辑使用，后续应在工作区干净且完成路由回归后再单独评估。

## 后续规则

1. 新页面先放入对应 feature，再注册到 `main_pages.json`，最后补充 `AppRoutes` 常量。
2. 未注册且无 import 的 `.ets` 文件只能进入清理候选，不能继续作为“历史壳”长期保留。
3. 持久化兼容代码不能仅因命名含有 `legacy` 就删除；必须先确认旧训练数据迁移方案。
4. 历史计划与验收记录可以保留，但不再作为当前路由或目录约束的来源。
